name: probe-runner

on:
  push:
    branches: [ main ]
    paths:
      - ".github/probe-runner.trigger"

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "push-triggered workflow scheduled"

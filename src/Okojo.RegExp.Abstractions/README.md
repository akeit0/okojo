# Okojo.RegExp.Abstractions

This project is no longer part of the public prerelease package set.

The regex engine contracts now live in `Okojo` itself, and public configuration should use `UseRegExpEngine(...)`.

Keep this project only as a temporary repo artifact until the remaining cleanup is finished.
